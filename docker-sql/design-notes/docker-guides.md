use  echo 'PS1="> "' > ~/.bashrc
to set your directory on your terminal to a short form
tes if your docker is working by running docker run hello-world

that is to just download a test image

if you add -it to your run, makes it intaractive

docker run -it ubuntu


